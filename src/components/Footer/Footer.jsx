import React from "react";

const Footer = ({className}) => {
  return (
    <section className={`relative overflow-hidden py-10 bg-teal-800 border border-t-2 border-t-black ${className}`}>
      Footer
    </section>
  );
};

export default Footer;
