import React from "react";

const Footer = ({className}) => {
  return (
    <section className={`relative overflow-hidden py-10 bg-teal-900/50 rounded-lg backdrop-filter backdrop-blur-md shadow-black shadow-2xl h-80 ${className}`}>
      Footer
    </section>
  );
};

export default Footer;
